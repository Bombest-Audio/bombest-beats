# Test-only deps. Install with: pip install -r requirements-dev.txt
# Kept separate from requirements.txt so the production Docker image stays lean.
pytest>=8.0,<9.0
pytest-flask>=1.3,<2.0
pytest-cov>=5.0,<6.0
moto[s3]>=5.0,<6.0
freezegun>=1.5,<2.0
